server {
    listen 80;
    server_name localhost;
    root /usr/share/nginx/html;
    index index.html;

    # Redirige todas las rutas al index para el enrutamiento de React
    location / {
        try_files $uri $uri/ /index.html;
    }
}
